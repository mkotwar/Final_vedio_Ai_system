# CUDA-enabled PyTorch stack for Windows and RTX 50-series GPUs.
# Install this before requirements.txt so the generic torch constraints do not
# select CPU-only wheels from PyPI.
--index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
torchvision==0.26.0
